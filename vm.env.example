# Copy to vm.env (gitignored). Used by start.sh / stop.sh and optionally deploy.sh push.

AZURE_RESOURCE_GROUP=glunova-rg
AZURE_VM_NAME=glunova-b4ms

# Optional: for ./deploy.sh push (VM can docker pull from ACR after az acr login on the VM)
# AZURE_ACR_NAME=glunovaacr
